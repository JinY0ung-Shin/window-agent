# Window Agent 환경변수 설정
# 이 파일을 .env로 복사한 후 값을 채워주세요: cp .env.example .env

# OpenAI API 키 (필수)
OPENAI_API_KEY=sk-your-api-key-here

# API Base URL (선택 — OpenAI 호환 서버 사용 시)
# OPENAI_API_URL=https://api.openai.com/v1

# 기본 모델 (선택 — 앱 설정에서도 변경 가능)
# OPENAI_MODEL=gpt-4o
