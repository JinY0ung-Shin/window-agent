# Node.exe Placeholder 자동 생성 - Consensus Plan

> Generated by /cplan (Claude + Codex consensus)
> Consensus reached: Round 2
> Date: 2026-03-17

## Requirement

build.rs에서 node.exe placeholder를 자동 생성하여 Windows/Linux 모두에서 clone 후 바로 빌드·실행 가능하게 수정

## Agreed Plan

- build.rs: CARGO_MANIFEST_DIR 기준으로 node.exe 없으면 빈 파일 자동 생성
- browser.rs: resolve_node_executable()에서 0바이트 파일 감지·무시, system PATH fallback

## Implementation Steps

1. build.rs 수정 — placeholder 자동 생성 {parallel: true}
   - Files: src-tauri/build.rs

2. browser.rs 수정 — placeholder 감지 로직 추가 {parallel: true}
   - Files: src-tauri/src/browser.rs

3. 빌드 검증 {parallel: false}
   - Dependencies: Step 1, 2

## Acceptance Criteria

- [x] node.exe 없는 상태에서 빌드 성공 (build.rs가 자동 생성)
- [x] 0바이트 node.exe는 런타임에서 무시되고 system PATH node 사용
- [x] 실제 node.exe(>0바이트)가 있으면 기존대로 해당 바이너리 사용

## Consensus History

| Round | Status | Key Changes |
|-------|--------|-------------|
| 1 | Major objections: 2 | Initial draft — placeholder만 생성, runtime 감지 누락 |
| 2 | Consensus | browser.rs 0바이트 감지 추가, CARGO_MANIFEST_DIR 사용 |

## Codex Final Assessment

- Strengths: 변경이 build.rs와 browser.rs에 잘 격리됨, CARGO_MANIFEST_DIR 사용으로 경로 안정성 확보
- Overall: 합리적이고 범위가 작은 수정, clone-and-build 크로스 플랫폼 지원 달성
